# Data Artifacts

Generated experiment files are written here and ignored by git:

- `math_subset.jsonl`
- `generated_traces.jsonl`
- `fhis_labels.jsonl`
- `step_hidden_states.pt`

The repository tracks only this note so the directory exists after checkout.
