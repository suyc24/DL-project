你要处理一份没有通过编译的 Lean 4 代码。你的主要目标仍然是检查目标步骤是否错误，而不是硬修 Lean。

只返回以下两种结果之一：

1. 如果错误只是语法、类型、tactic、Nat/Int/Real 转换、库接口等形式化问题，返回一个 Lean 代码块，包含完整文件和 `import Mathlib`。
2. 如果在修复过程中发现目标步骤不可靠，直接返回合法 JSON：
{
  "verdict": "invalid",
  "reason": "...",
  "confidence": 4
}

输入会包含：
- 原 Lean 代码；
- Lean 输出/错误；
- fail_decide 阶段给出的 repair_instruction。

正确性判定：
- 判断对象是“目标步骤这句话是否严格可靠”。
- 你要逐字检查目标步骤原文的结论和推理细节，而不只是检查它最后的小结论是否可以成立。
- 不要为了使用 Lean 证明结论，自己补充、改写或修正目标步骤原文没有给出的证明细节。
- 如果目标步骤的小结论可以用额外推理补出来，但目标步骤原文的推理细节不对、遗漏关键理由、使用错误理由、偷换条件或结论过强，仍然应判定为不严格可靠。
- 如果你发现目标步骤明显错误、缺失关键前提、推理理由不成立，或必须添加目标步骤没有给出的关键 `h_missing_*` 才能证明，直接返回 invalid JSON，不要硬修 Lean。

Lean 处理：
- theorem 名字必须使用给定名称。
- 保持目标步骤原文表达的命题和推理依赖不变，不要把目标改成正确版本、反例命题、或“该步骤错误”。
- 如果错误只是语法、类型、tactic、Nat/Int/Real 转换、库接口等形式化问题，请修复 Lean 代码。
- 可以把题目和前文中已经明确给出的条件写成 theorem 参数。
- 如果目标步骤依赖前文没有明确给出的关键前提，可以加入局部假设 `h_missing_*`，但它必须是具体缺失前提，不能直接断言最终结论；这类假设会被视为目标步骤可能不严格可靠的证据。
- 不使用 `sorry`、`admit`。
- warning 不需要处理。
- 所有注释使用中文。

总原则：
- 强调：你的目标不是让 Lean 尽量通过，而是为后续判断提供忠实证据。即使目标步骤的小结论可以用额外推理补出来，只要目标步骤原文的推理细节不对，也不能把它形式化成修正后的正确证明。
