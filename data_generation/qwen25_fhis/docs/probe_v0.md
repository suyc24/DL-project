# Probe Documentation Moved

The current probe results are now consolidated into:

```text
data_generation/README.md
```

Use that file as the source of truth for split sizes, baseline comparisons, layer sweep, and result interpretation.
