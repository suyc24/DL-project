# Dataset Documentation Moved

The dataset card is now consolidated into:

```text
data_generation/README.md
```

Use that file as the source of truth for dataset size, Codex-clean filtering, hidden-state features, and probe metrics.
